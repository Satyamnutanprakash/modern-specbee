{%
  set title_classes = [
    label_display == 'visually_hidden' ? 'visually-hidden',
  ]
%}

{{ attach_library('specbee_theme/slider-block')}}
{{ attach_library('specbee_theme/slick')}}


{% if label_hidden %}
	{% if multiple %}

			{% for item in items %}
				<div{{item.attributes}} class="slider__icon">
					{{item.content}}
				</div>
			{% endfor %}
	{% else %}
		{% for item in items %}
			<div{{item.attributes}} class="slider__icon">
				{{item.content}}
			</div>
		{% endfor %}
	{% endif %}
{% endif %}

